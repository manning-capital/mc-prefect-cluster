[project]
name = "prefect-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "prefect>=3.4.5",
    "prefect-docker>=0.6.6",
]

[dependency-groups]
dev = [
    "ruff>=0.11.13",
]
